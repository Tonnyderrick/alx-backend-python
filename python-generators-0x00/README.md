# ALX Backend Python - seed.py

This project is part of the `python-generators-0x00` directory in the ALX Backend Python curriculum.

##  Objective

The goal is to create a Python script that:
- Connects to a MySQL server
- Creates a database `ALX_prodev` if it doesn't exist
- Creates a table `user_data` with specified fields
- Populates the table using data from a CSV file (`user_data.csv`)
- Provides a structure that allows for scalable data streaming using
